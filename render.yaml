services:
  - type: web
    name: server
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    plan: free
    instances: 1
    autoscale: false
    regions:
      - oregon
    httpPort: 10000
